<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
  <defs>
    <linearGradient id="sky" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#38BDF8"/>
      <stop offset="100%" stop-color="#0284C7"/>
    </linearGradient>
  </defs>

  <!-- Sky blue gradient background -->
  <rect width="128" height="128" rx="24" fill="url(#sky)"/>

  <!-- Floppy disk body — white with classic notched top-right corner -->
  <path d="M20 20 L84 20 L108 44 L108 108 L20 108 Z" fill="white"/>

  <!-- Notch corner fill (shows depth) -->
  <path d="M84 20 L108 44 L84 44 Z" fill="#7DD3FC"/>

  <!-- Label area (upper-left of disk) -->
  <rect x="28" y="26" width="44" height="30" rx="3" fill="#BAE6FD"/>

  <!-- Metal shutter (lower portion of disk) -->
  <rect x="36" y="68" width="56" height="34" rx="4" fill="#0284C7"/>

  <!-- Shutter read/write slot -->
  <rect x="50" y="74" width="28" height="22" rx="2" fill="white" opacity="0.30"/>
</svg>
